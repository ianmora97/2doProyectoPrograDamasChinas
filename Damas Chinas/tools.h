#pragma once

#include <iostream>
#include <string>
#include <sstream>
#include <Windows.h>
#include <time.h>
#include <fstream>

using namespace std;

void gotoxy(int, int);
void color(int);
void pause();
void cls();
int checkInt(int, int);
